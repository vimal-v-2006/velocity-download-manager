export function bytes(value: string | number): number { const n = typeof value === 'string' ? Number.parseInt(value, 10) : value; return Number.isFinite(n) ? n : 0; }
export function formatBytes(value: string | number): string { const n = bytes(value); if (n <= 0) return '0 B'; const units = ['B','KB','MB','GB','TB']; const i = Math.min(Math.floor(Math.log(n) / Math.log(1024)), units.length - 1); return `${(n / 1024 ** i).toFixed(i === 0 ? 0 : 1)} ${units[i]}`; }
export function formatSpeed(value: string | number): string { return `${formatBytes(value)}/s`; }
export function progressPercent(completed: string | number, total: string | number): number { const c = bytes(completed); const t = bytes(total); if (t <= 0) return 0; return Math.max(0, Math.min(100, (c / t) * 100)); }
export function formatEta(completed: string | number, total: string | number, speed: string | number): string { const left = bytes(total) - bytes(completed); const s = bytes(speed); if (left <= 0) return 'done'; if (s <= 0) return '∞'; const sec = Math.ceil(left / s); const h = Math.floor(sec / 3600); const m = Math.floor((sec % 3600) / 60); const r = sec % 60; if (h) return `${h}h ${m}m`; if (m) return `${m}m ${r}s`; return `${r}s`; }
export function fileNameFromItem(item: { files?: { path: string }[]; bittorrent?: { info?: { name?: string } } }): string { return item.bittorrent?.info?.name || item.files?.[0]?.path?.split(/[\\/]/).pop() || 'New download'; }
